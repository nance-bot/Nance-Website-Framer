# Nance Website

Static landing page for Nance, built from the Figma Final Design (no Framer).

## Local

Open `index.html` in a browser, or serve the folder:

```bash
npx serve .
```

## Assets

Figma exports live in `assets/` (logos, store badges, section visuals, chart, cards).
